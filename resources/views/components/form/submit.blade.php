<div class="form-control-2">
    <div class="text-center">
        <button type="submit" class="btn btn-danger">
            <i class="fa fa-btn fa-save"> {{$name}}</i>
        </button>
        <a href="{{$url}}" class="btn btn-primary">Vazgeç</a>
    </div>
</div>